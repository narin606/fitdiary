# FitDiary progress

## 2026-09-15 — persistent date-aware core workflows (bounded local slice)

- Source scope: added strict UTC `YYYY-MM-DD` day parsing/bounds and authoritative aggregate calculations; expanded authenticated diary and weight/water/exercise routes with user-owned date reads and create/update/delete operations; added one composed `/tracking/day-summary` response; added a shared web API client and replaced the protected dashboard's hard-coded September 9 totals with selected-day persisted data and food/water/weight/exercise forms.
- Safety scope: reused the existing Prisma models without migration; preserved auth/session and pending-registration code; made no infrastructure, database, credential, deployment, AI-provider, photo-storage, or production changes. Food saving contains no AI call.
- Deterministic evidence: new aggregate/date tests first failed because `daySummary.js` did not exist. Route persistence integration initially failed against the pre-fix API when an unknown `foodId` reached PostgreSQL's foreign-key constraint and terminated the process. The diary route now accepts a supplied `foodId` only when it resolves to a global `VERIFIED`/verified food or the authenticated user's `CUSTOM` food; snapshot name/nutrients remain request-owned and no AI path is invoked.
- Isolated database evidence: `npm run test:integration -w @fitdiary/api` creates a disposable `postgres:16-alpine` container with a dedicated database/user on loopback port `55432`, sets its own explicit `DATABASE_URL`, applies both migrations from empty, builds and starts the API on loopback port `4401`, runs one database-backed route integration scenario, and removes the API/container through an exit trap. The harness does not source `.env.production` or any `.env` file. The passing scenario proves two authenticated users cannot read or mutate each other's records, exact UTC inclusive-start/exclusive-end date reads, diary and water/weight/exercise create/read/update/delete, exact day-summary totals, owned/custom and global-verified food acceptance, snapshot preservation, and unknown/cross-user/unverified-global food rejection.
- Ordered final gates: no lint script is configured; `npm run build` passed (Next.js production build/type validation and API TypeScript build); API unit tests passed 18/18; PostgreSQL integration passed 1/1; web tests passed 3/3; `git diff --check` passed. No browser suite, deployment, production database test, or release is claimed.

## 2026-09-15 — QA functional-completion audit

- Verified the QA report against source at `a6ecbe6`: the authenticated product is a single client-side dashboard with hard-coded September 9 data and local-only section/modal/water state. The existing API has partial write endpoints for diary, food, water, weight, and exercise, but the web client calls none of them; no composed day-summary/read endpoints exist. This confirms the core P1 issues are product implementation gaps, not merely an unavailable server environment.
- Confirmed deployment boundary: API + PostgreSQL run in the FitDiary Compose project; the frontend is separately Vercel-hosted. The most recent API change changed login to `{ identity, password }`, while the public browser bundle must be deployed together (or API compatibility temporarily retained) before it can be called a working browser release.
- Safety gate: the requested completion requires new persistent read/write API contracts and likely a Prisma migration. The currently live database now contains the newly verified owner account, so do **not** repeat the earlier empty-database reset. Before a production migration, make a scoped backup of the FitDiary volume/database and preserve the verified account.
- External hard blocker identified for QA-06/AI: private photo upload/storage is not implemented and no object-storage provider/configuration is present in the running app. It cannot truthfully be claimed working until a specific private-storage target and credentials are configured. AI analysis must remain disabled until an uploaded private photo is present and user explicitly requests analysis.
- User approved a self-hosted storage path. Re-mounted the existing 2 TB ext4 disk from the non-persistent desktop automount to `/data` through an `/etc/fstab` UUID record with `nosuid,nodev,noexec`; no format or data deletion occurred. Created `/data/fitdiary/uploads` as mode `0700`, owned by API runtime UID/GID `1000:1000`, and proved a non-root API image process can create/remove a file there. Added the host bind mount to the API Compose service only (`/data/fitdiary/uploads:/app/uploads:rw`); PostgreSQL/web/proxy receive no mount. Compose config validates. Public probes to likely static paths returned `404`; the directory is not publicly routed. Production API has not yet been recreated with the mount because the secure upload routes/schema are still being implemented.

## 2026-09-15 — pending-only account activation and delivery recovery

- Security model: a registration no longer creates a `User`. It creates only an expiring `PendingRegistration` containing the Argon2id password hash and SHA-256 verification-token hash. The verification endpoint conditionally consumes the delivered pending token and creates a `User` with `emailVerifiedAt` in the same transaction.
- Abuse controls: valid registration responses remain generic; verified/pending identity collisions do not send another email or create an account. Registration is throttled per source IP, username, and email in a fifteen-minute window. Failed email delivery removes the pending registration; therefore delivery failure cannot reserve an account identity.
- Delivery diagnostics: Resend outcomes emit structured, redacted status/category and provider-message-ID events. Secrets, recipient addresses, passwords, and raw tokens are not logged.
- Schema: replaced the pre-account `EmailVerificationToken` relation with `PendingRegistration`. The additive production migration removes obsolete pre-account tokens and introduces pending-record uniqueness and expiry indexes.
- Local deterministic evidence: Prisma Client generation, API TypeScript build, API unit tests (14/14), frontend production build, and `git diff --check` passed. A disposable PostgreSQL 16 database applied both migrations from empty to latest and validated the resulting `User` + `PendingRegistration` schema; no `EmailVerificationToken` table remained.
- Production maintenance completed with explicit authorization: archived the former FitDiary named database volume at `/home/brandon/backups/fitdiary/` (SHA-256 recorded in the maintenance transcript), removed only that FitDiary volume, then rebuilt/recreated only `fitdiary-prod` with `.env.production`—no broad Docker cleanup and no other app touched. The new database is empty (`User=0`, `PendingRegistration=0`) before the controlled flow; API and DB are healthy, the expected named volume is attached, and the live PostgreSQL/API configuration fingerprints match `.env.production`.
- Controlled live delivery: Resend accepted one owner-mailbox verification request (provider message ID recorded only in redacted service logs). The public registration endpoint returned the generic `202` response; immediately afterward `User=0` and `PendingRegistration=1`, proving a registration does not create an account before the link is consumed. Inbox receipt and link-click activation remain the final user-operated E2E gate.
- Login identity update: usernames remain canonicalized lowercase and database-unique for both pending and verified records. Login now accepts either that unique username or the verified, database-unique email address plus password; the client label/validation and request contract use `Username or email`. API tests (14/14), web tests (3/3), API/web production builds, and whitespace validation passed before release.

## Historical record

Previous entries documented the retired pre-account verification design. They are superseded by the 2026-09-15 pending-only activation model above.
