"use client"; import Link from "next/link"; import { FormEvent,useState } from "react"; import { authApi } from "../../../lib/api"; import { AuthCard,Field } from "../../../components/AuthCard";
export default function Forgot(){const[email,setEmail]=useState("");const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);try{setMsg((await authApi.forgotPassword(email)).message)}catch{setMsg("If an eligible account exists, password reset instructions will be sent.")}finally{setBusy(false)}}return <AuthCard title="Reset your password" subtitle="Enter the email connected to your account."><form onSubmit={submit}><Field label="Email" type="email" value={email} set={setEmail} auto="email"/>{msg&&<p role="status" className="auth-alert">{msg}</p>}<button disabled={busy} className="auth-submit">{busy?"Sending…":"Continue"}</button></form><p className="auth-switch"><Link href="/login">Back to login</Link></p></AuthCard>}