"use client"; import Link from "next/link"; import { FormEvent,useState } from "react"; import { unavailablePasswordMessage } from "../../../lib/api"; import { AuthCard,Field } from "../../../components/AuthCard";
export default function Forgot(){const[email,setEmail]=useState("");const[msg,setMsg]=useState("");function submit(e:FormEvent){e.preventDefault();setMsg(unavailablePasswordMessage)}return <AuthCard title="Reset your password" subtitle="Enter the email connected to your account."><form onSubmit={submit}><Field label="Email" type="email" value={email} set={setEmail} auto="email"/>{msg&&<p role="status" className="auth-alert">{msg}</p>}<button className="auth-submit">Continue</button></form><p className="auth-switch"><Link href="/login">Back to login</Link></p></AuthCard>}