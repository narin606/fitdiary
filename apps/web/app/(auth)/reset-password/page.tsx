"use client"; import Link from "next/link"; import {FormEvent,Suspense,useState} from "react"; import {useSearchParams} from "next/navigation"; import {authApi} from "../../../lib/api"; import {AuthCard,Field} from "../../../components/AuthCard";
function ResetForm(){const token=useSearchParams().get("token")??"";const[password,setPassword]=useState("");const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);try{setMsg((await authApi.resetPassword(token,password)).message)}catch(error){setMsg(error instanceof Error?error.message:"Invalid or expired password reset link.")}finally{setBusy(false)}}return <AuthCard title="Choose a new password" subtitle="Secure your FitDiary account."><form onSubmit={submit}><Field label="New password" type="password" value={password} set={setPassword} auto="new-password"/><p className="auth-hint">Use 10–128 characters.</p>{msg&&<p className="auth-alert" role="status">{msg}</p>}<button disabled={busy||!token} className="auth-submit">{busy?"Saving…":"Reset password"}</button></form><p className="auth-switch"><Link href="/login">Back to login</Link></p></AuthCard>}
export default function Reset(){return <Suspense fallback={<AuthCard title="Choose a new password" subtitle="Loading…"><p>Please wait.</p></AuthCard>}><ResetForm/></Suspense>}