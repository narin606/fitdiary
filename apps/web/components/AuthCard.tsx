export function Field({label,value,set,error,type="text",auto}:{label:string,value:string,set:(v:string)=>void,error?:string,type?:string,auto:string}){const id=label.toLowerCase().replace(" ","-");return <label className="field" htmlFor={id}>{label}<input id={id} type={type} autoComplete={auto} value={value} onChange={e=>set(e.target.value)} aria-invalid={!!error} aria-describedby={error?`${id}-error`:undefined}/>{error&&<span id={`${id}-error`}>{error}</span>}</label>}
export function AuthCard({title,subtitle,children}:{title:string,subtitle:string,children:React.ReactNode}){return <div className="auth-card"><div className="mobile-brand">🔥 FitDiary</div><h2>{title}</h2><p>{subtitle}</p>{children}</div>}
